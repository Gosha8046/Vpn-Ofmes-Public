#pragma once

// Single source of truth for the application version, also used to
// generate config/version.json when cutting a release. Keep in sync with
// the `project(VpnOfmes VERSION ...)` line in CMakeLists.txt.
#define VPNOFMES_VERSION "1.0.0"
